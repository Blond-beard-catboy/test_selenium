version: '3.8'
services:
  api:
    build: ./api
    ports:
      - "5000:5000"
  selenium:
    image: selenium/standalone-chrome
    ports:
      - "4444:4444"
    volumes:
      - ./selenium-tests:/tests
    depends_on:
      - api